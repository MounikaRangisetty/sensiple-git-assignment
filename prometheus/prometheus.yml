global:
  scrape_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['localhost:9093']  # Address of your Alertmanager

rule_files:
  - 'alert_rules.yml'  # Your alert rules file

scrape_configs:
  - job_name: 'my_python_app'
    static_configs:
      - targets: ['http://13.233.92.61:4000/metrics']  # Your application metrics endpoint
